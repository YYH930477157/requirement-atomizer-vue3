# 交接文档：next-steps 计划执行结果与后续路线（2026-08-31）

> 读者：接手的代理（Claude Code/GLM/grok 等）与审核方。本文自包含：环境事实、
> 全部结论、成本账、资产位置、下一步工作清单与现成命令。假设读者未参与此前
> 会话。
> 上游文档：`docs/next-steps-execution-summary-2026-08-30.md`（执行总结论）、
> `docs/outline-authority-validation-2026-08-29.md`（flag 验证报告）、
> `docs/ws0-gate-result-2026-08-30.md` + `docs/ws0-gate-result-v2-2026-08-30.md`
> （门禁两次归档）、`CLAUDE.md` 2026-08-29b/08-30b/08-30c 条目。

---

## 0. 环境事实（执行前核对）

| 项 | 值 |
|---|---|
| 主检出 | `E:\Codex\requirement-atomizer-vue3`，main（本文定稿时 HEAD 在 f6a0b0c 后续） |
| 全量测试 | `python tools/run_tests_parallel.py`，~45s，**4227+ OK / skipped=1**（主检出口径，golden 实跑零漂移） |
| LLM key | deepseek，**余额以 `/user/balance` 实查为准**（2026-08-31 时点 ¥31.49，`is_available` 真实值见接口）；模型仅 `deepseek-v4-flash` / `v4-pro` / `v4-flash-vision-exp`，**无非推理兄弟模型** |
| SBD 真实语料 | `C:\Users\YYHwudi\Desktop\Canna-29\1780709839_SBD_ZETDC\`（PDF + result/result2/**result3**[只读基线] + result-outline-on[flag 开验证包]） |
| ABNT 语料/产物 | `C:\Users\YYHwudi\Desktop\Canna-29\Appendix 9-ABNT NBR 16968-2022 EN.docx`；parsed 产物 `out/abnt_nbr_16968/`；golden 基线 `out/abnt_nbr_16968_atomizer_v5/`（三 seed KB 生成，勿用错 KB 重生成） |
| 门禁材料 | truth `golden_sets/ws0_human_v1/truth.jsonl`（190 行）/ thresholds（14 键）/ 模板 `C:\Users\YYHwudi\Desktop\Canna-29\电表软件标准化需求列表-V2.3.12 - 2026-4-14..xlsx` |
| **门禁温缓存** | `%TEMP%\ab-runner.9gqsr665\A_atoms\ai_extract_cache.jsonl`——**全量 343 节** A 轨抽取缓存（2026-08-30 门禁 v2 产出），再跑全门禁 A 抽取零付费 |
| 回放脚本 | `out/tools/replay_outline2b.py`（边界回放）、`out/tools/replay_outline2b_attribution.py`（归因底账）、`out/attribution-result3/`（result3 scratch 副本）——out/ 不进仓，机器本地 |

## 1. 本次执行完成的事（均已合 main 并 push）

| 工作 | 提交 | 内容 |
|---|---|---|
| 任务 C：队列投影审计工具 | `3d9d79e`（实现在 `65fe90f`） | `tools/audit_review_queue_projection.py` 只读对账（exit 0/2/3）+ 12 测试 |
| 修复 1+2：路由连坐窄门修 + 义务碎片过滤 | `0d9adfe`（`275a6be`） | routing v7（tender 路由出前按 confirmed 吞并 heading 纯切分）+ conservation v6（内容词<2 碎片剔基线，`fragment_units_excluded` 审计） |
| 修复 3：模板写入器列位 | `17e6ee3`（`a589119`） | template_writer/v2 按表头解析列位 + 正文兜底链；门禁数据回归 60→0 空正文 |
| max_tokens floor | `e1c1fee`（`1429ea8`） | `PURPOSE_MIN_TOKENS["extract"]` 与 `AI_EXTRACT_MIN_MAX_TOKENS` 同步 6144→24576；B 轨功能直抽接 `apply_min_tokens`（此前 4096 裸奔） |
| 验证/门禁/总结文档 | `1edf231`/`f6a0b0c` | flag 验证报告（No-Go）、门禁 v1/v2 归档、执行总结、CLAUDE.md 条目 |

## 2. 结论（四条，均已落盘文档）

### 2.1 大纲权威 flag（RATOMIZER_OUTLINE_AUTHORITY）——No-Go，默认保持 0

五条 Go 判定线过四：块守恒 821=821 零违例、新切条款 FRE 抽样 10/10 无借位、
ABNT 重切克制（358→361）、duplicates 6→0 / binding 50→30 / evidence 15→6 收敛
（主要归功中间版本，非 flag）；**卡在义务覆盖**：统一碎片口径实质义务未覆盖
18（result3）→22（flag 开真实运行），对同代码 flag 关基线（6）恶化且归因混合
（新边界×新 LLM 输出不可分）。病灶条款 BLK-000240 修复本身成功（切出 + 义务
零未覆盖）。**计划书 §1.1 的「207→239」是笔误，代码事实 207→339**（与 `bef6a86`
提交信息吻合）。若重启翻转评估：先做 §3 修复方向，并在 routing v7/conservation v6
基线上重新测量（v7 已以远小爆炸半径修复了「内容连坐丢失」这一 flag 主要病灶——
result3 重放实证 BLK-000240 在 **flag 关现状下**回抽取池、抽取条款 95→102、
义务未覆盖 12→7）。

### 2.2 WS0 门禁——两次重跑均 FAIL，但失败面已从基础设施收敛为真实质量项

- **v1**（¥36.43，透支 402 截胡）：实证 08-17 两旧根因已修（A 轨成文 932 行可读；
  B 轨守恒闭合——后证是 stub 截短产物上的假象）；新暴露 A 轨 60 条空正文行。
- **v2**（¥17.80，≤¥20 达标，无 402/stub）：**A 轨首次完整通过**（902 FRE/894 行，
  template_writer v2 实证）；B 轨 176/176 条款完整抽取（225 FRE），守恒闸拦下成文——
  **obligation 0 未覆盖（v7+v6 实证）、duplicates 0，剩 binding_mismatches=25
  （`narrative_covers_other_clauses_not_declared` 跨条款借位为主）+
  preservation blocking=3（Security 节 no/5/64）**。truth 阈值门（14 键）尚未评估
  （failures 短路）。

### 2.3 成本口径（后续预算按此校准）

- **deepseek-v4-flash 是推理模型：max_tokens 记思考+答案**（实证：241 调用
  completion 分布被 4096/8192/16384 帽子整齐压住、p95=8191、零超 16384；输出
  token 88% 是隐藏思考）。端点无思考开关、无非推理模型。
- floor 24576 后 B 轨单条款 ¥0.041→¥0.026；A 轨 343 节全冷 ≈¥28-30，全暖 0；
  **门禁全量重跑（A 全暖）≈¥5-8**。
- key 纪律：只进进程环境；`out/ab-gate-*.json` 的 env_snapshot 含明文 key
  （out/ 不进仓，勿移动位置）。

### 2.4 资产

审计工具（兼容期随时体检投影一致性，result3 实测零写入）；门禁温缓存（全量
343 节）；回放脚本群；四份归档文档。

## 3. 已知债务（合并时声明，见 CLAUDE.md 2026-08-30b）

1. `DOCUMENT_OUTLINE_VERSION` 未并入路由指纹——routing v7 的 veto 依赖大纲裁决，
   大纲升级时路由输出会静默变化（违反「指纹含确定性后处理版本」纪律）；
2. 写入器与门禁读取器两套列解析（header-first vs alias-first）应收敛为共享列契约权威；
3. 义务碎片判据（内容词<2）为 SBD 单语料标定，换语料族按 `fragment_units_excluded`
   审计重校准。

## 4. 下一步工作清单（按优先级）

1. **门禁 B 轨 28 条失败项逐条归因**（工作目录 `%TEMP%\ab-runner.9gqsr665\B_direct`）：
   25 绑定失配（借位叙述 vs 无义务条款上的 FRE——prompt 或绑定校验语义缺口）+
   3 preservation（Security 数值/否定）。修后再跑门禁（≈¥5-8，A 全暖命令见 §5）；
   B 过守恒后 truth 阈值门第一次真正评估。
2. **PASS 后**：提 `RATOMIZER_EXECUTION_POLICY` 默认翻转议案（单独立项，含回滚通道）。
3. **大纲 flag 翻转评估重启**（可选）：先修 §3 债务 1；短条款抽取覆盖强化；
   heading-only 退化 objective；可选补「当前代码 flag 关」付费对照腿（≈¥7）。
4. **任务 D：技术表格保留率诊断**（零成本，未启动）——SBD preservation blocking
   50 项归类（a 行级 FRE 在但叙述丢数字 / b 无 FRE / c 输入侧没带到）+ 四候选
   设计文档（行/格粒度抽取、确定性行展开、专家定点、data_constraints 挂载——
   候选 4 有代码级依据：直抽 prompt 已含该语义、preservation 记账扫描
   data_constraints）。
5. **队列投影兼容期观察**：真实结果包定期跑审计工具，drift 零容忍；两个桌面
   发布周期后出读者迁移盘点（含启动/GET 自动对账前置件，见
   `docs/review-queue-convergence-design-2026-08-27.md` §6）。
6. worktree 清理（可选）：`E:\Codex\ratomizer-wt-{queue-audit,outline-veto,twriter,maxtok}`
   分支均已合并，可 `git worktree remove`。

## 5. 现成命令

```powershell
# 门禁重跑（A 全暖 ≈¥5-8；PYTHONPATH 与 clause_family 必设；key 只进进程环境）
$env:PYTHONPATH='.'; $env:RATOMIZER_CONTEXT_PACK_STRATEGY='clause_family'; $env:RATOMIZER_LLM_API_KEY='<key>'
python tools/ab_runner.py --parsed-dir out/abnt_nbr_16968 `
  --template "C:\Users\YYHwudi\Desktop\Canna-29\电表软件标准化需求列表-V2.3.12 - 2026-4-14..xlsx" `
  --route openai_compatible --truth golden_sets/ws0_human_v1/truth.jsonl `
  --thresholds golden_sets/ws0_human_v1/thresholds.json `
  --out out/ab-gate-report-rerun3.json `
  --warm-a-cache "$env:TEMP\ab-runner.9gqsr665\A_atoms" --keep-dirs
# 注意：--warm-a-cache 传【目录】（A_atoms），传文件路径会在付费前 fail-loud

# 队列投影审计（只读）
python tools/audit_review_queue_projection.py --out-dir <结果包>

# 边界/归因回放（零成本，out/ 本地）
python out/tools/replay_outline2b.py [--pkg <结果包>]
python out/tools/replay_outline2b_attribution.py
```
