# AGENTS.md

Context for agents working in this repo. `CLAUDE.md` is the historical decision log — read it for context before non-trivial work, but it may contain stale claims; where it conflicts with current code/config, the code wins (newest dated entries are most reliable). Update it after milestones.

## What this is

Python pipeline that atomizes technical standards (DOCX/XLSX/PDF) into reviewable atomic requirements, ending in DLMS/COSEM implementation specs. Flat layout: most modules are top-level `*.py` files (registered in `pyproject.toml` `py-modules`); packages are `parsers/`, `requirement_kb/`, `gui/`, plus `ui/` (Vue3+Electron).

Key entrypoints: `cli.py` (`ratomizer`), `atomize.py`, `api_server.py` (local review API), `desktop_tasks.py` (Electron task bridge). Machine-facing CLI contract: `docs/cli-contract.md` (exit codes 0/2/3/4, stdout = UTF-8 JSON envelope).

Agent Phase 0/1/1.5: `agent_eval.py` runs `golden_sets/agent_eval_v1`; `agent_state.py` builds a read-only artifact view; `agent_loop.py` runs the bounded loop (`--decider rule` default, `--decider llm` opt-in with per-iteration rule fallback and decision-only token accounting); `agent_compare.py` runs rule vs llm on copies; `decide_trace.py` validates/appends `decide_trace.jsonl`. The cache lineage anchor is `agent_policy.AGENT_POLICY_VERSION` (`agent-policy-v3`). Agent stages are deliberately outside `CHAIN_ORDER`.

Agent Phase 2: `llm_client.chat_with_tools` runs a bounded tool-loop (`max_rounds=8`, per-requirement token budget default 20000, over-budget/round-cap/4xx → existing stub failure path, never disguised as reviewed); `review_tools.py` exposes five deterministic read-only review tools (`kb_search`/`kb_get`/`blue_book_class`/`source_read`/`coverage_check`) with `REVIEW_TOOLS_VERSION` (`review-tools-v3`) pinned into the review cache fingerprint; `llm_agents/review_pipeline.yaml` operations carry `executor` (`tool_loop` for classify_risk/correct_errors as one fused call per requirement, `deterministic` for merge_duplicates/gap_find, `deferred` for test_point_generate). WP2: unfounded enrichment fields are marked `待澄清` in `requirements_analysis._apply_llm_item` (`UNFOUNDED_RULE_VERSION=analyze-unfounded-v3`, in the analyze cache fingerprint; `ANALYZE_PROMPT_VERSION=analyze-llm-v7`).

Word/Excel facsimile branch + spot extract (2026-07-28, spec `docs/facsimile-spotextract-spec.md`): `doc_facsimile.py` lazily converts docx/xlsx to `out/document_facsimile.pdf` at the `export-annotation-html` stage (Office COM first, LibreOffice `soffice` fallback, `unavailable:<reason>` recorded honestly — page images are never faked); cache key = input sha256 + `DOC_FACSIMILE_VERSION` (`doc-facsimile-v1`), which is pinned into the `export-annotation-html` chain producer stamp. `spot_extract.py` + `POST /spot-extract` (alias `/api/spot-extract`) runs single block/row targeted analysis: deterministic guards-v16 single-row expansion for requirement-shaped parameter rows, single-segment `critique_section` LLM path otherwise; drafts (`source_mapping: "spot_extract"`, suspicion `用户定点解析`) go to clarification only, and an unavailable LLM fails loudly (`ok: false`) — never a fabricated stub.

Table structure & cell-level closure (2026-08-01, branch `codex/table-structure-cell-closure`): `table_structure.py` owns deterministic table roles and row/cell/mixed leaf planning (`TABLE_STRUCTURE_VERSION="table-structure-v7"`; published closure baseline catalog `claim-catalog-v11`). `table_cell_items.jsonl` (schema `table-cell-item/v1`) records each non-empty physical cell/merge anchor and is hash-bound into publication. Table-cell claims use exact `table_cell_text` locators, per-sentence spans, deterministic row/header context, and a real catalog→publish→fold→queue→execute→annotation E2E. Ambiguous, weak-signal, unsignaled, rejected-marker, and untyped-colon cells are materialized as reviewable excluded candidates rather than silently dropped or promoted. Experts can promote them or confirm exclusion; the published baseline writes `claim_structural_candidate_decisions.jsonl` v2 with frozen v1 replay, and pending candidates block Ledger Ready. Old table artifacts are never fake-migrated: version/status gates return `base_migration_required`.

Structured full-table translation (2026-08-11, branch `codex/table-translation-structure`; superseded in parts by the 2026-08-14 overhaul below): `full_translation.py` uses `FULL_TRANSLATION_VERSION="full-translation-v2"` and emits `document-translation/v2`. Regular tables translate caption/header/data units by the shared `_row_render_line` content hash and render bilingual `figure/table/thead/tbody`; synthetic `column_N` headers never enter prompts or HTML. Horizontal single-row merges retain `colspan`; nested or vertical-merge tables degrade honestly. `TABLE_STRUCTURE_VERSION="table-structure-v8"` recognizes sequential `(a)..(j)` inline/stacked composite headers and removes letter prefixes from effective display headers while retaining evidence.

Repo-wide efficiency/effectiveness overhaul (2026-08-14, same branch, uncommitted; full entry in CLAUDE.md): versions now `table-structure-v9`, `docx-table-physical-v2`, `table-disposition-rules-v3` (both pinned into the atomize producer), `full-translation-v3`/`document-translation/v3` (vertical merges stay structured with inherited-anchor full-column rows — no rowspan; stacked title rows via new `title_rows` block payload; empty/non-translatable rows skipped; `_looks_translatable` accepts any non-CJK letter script), `ai-extract-v25` + `enrich-v4` (compact JSON prompts), `analyze-unfounded-v4` (per-field fabricated-code downgrade, red line intact), `review-tools-v5` (memoized consistency indexes, ~68x per call), `llm-review-cache-v7` (per-requirement evidence scoping; coverage_check rows stay whole-file). Append-only rewrite: attempt log/verifier WAL (~370x on 300 events, compaction env thresholds registered in `config.ENV_REGISTRY`), enrich cache JSONL (`analyze-enrich-cache-v3`), translation sidecar journal (main JSON format unchanged). Hot paths: `load_committed_effective_snapshot_cached` for `/table-reviews`+`/document/pdf`, memoized `/ai-extraction-status`, slice-before-enrich `/requirements`, single-hash stage fingerprints, marker-contract cache, background startup maintenance after readiness. `llm_client` disables a tool after 2 consecutive same-tool errors instead of aborting paid rounds. `review_state`/`desktop_tasks` os.replace unified to 8-attempt linear backoff; burst-coalesced `fold_effective_ledger`; PID-liveness stale-lock stealing; UI auto-POSTs `/claim-maintenance` on recovery-pending 503 then replays. merge-to-main requires the golden `out/` regeneration workflow (multiple behavior versions bumped).

Review closeout five fixes (2026-08-16c, same tree; default is now 1 per project decision — "enabled by default, controlled operation"; full entry in CLAUDE.md): P1 targeted-reextract now refreshes the same-fingerprint cache row with the merged product (stale-cache restore can no longer silently revert a reextract); P1 cross_script_review obligations became BLOCKING internal-check clarification questions (block readiness/full closure until expert verified_ok); P1 XLSX preservation-rate denominators now span ALL truth rows (unmatched truths count as unpreserved); P2 claim source anchors verify `source_text_hash` against obligations recomputed from current chunks on both publish and replay (mismatched anchors dropped/stale, cannot close claims); P2 truth↔row matching is now Hungarian max-weight bipartite matching (max TP → max total score → deterministic (truth_id, row) tie-break; brute-force cross-checked).

De-atomization remediation M1-M3 (2026-08-16, same branch, three parallel threads; full entry in CLAUDE.md): M1 — obligation coverage is now local-binding only (`functional_extract._obligation_evidence_edges`; eligible = declared-blocks intersect the obligation's clause; edge methods lexical > cross_script_review (coverage + manual-review list) > source_quote (anchor only, NOT coverage); single obligation unit must be covered by ONE eligible FRE; versions `functional-extract-guards-v4` + `functional-conservation-obligation-evidence-v2`). M2 — `functional_reextract.py` functional_targeted_reextract (clause-family re-extract, unaffected FREs byte-stable, only ok+conserved replaces atomically) + claim queue dispatches per resolved store (all CAS/WAL/budget machinery shared) + coverage edges carry `target_source_anchors` with locator-jurisdiction stale checks; queue E2E reaches full closure automatically. M3 — ab_runner computes precision/recall/F1 via deterministic one-to-one matching against the final 软件需求列表-成文.xlsx, 6 preservation rates, 14 mandatory per-document thresholds; missing truth/thresholds → NO_GATE (exit 1). M4 (real corpus, golden rebuild, default flip) remains gated on the plan's Go/No-Go checklist.

De-atomization overhaul (2026-08-15b, same branch, on top of main `8389e1d`; full entry in CLAUDE.md): implements plan §3.1-§3.6 — `functional_extract.py` conservation is now obligation/evidence many-to-many (`functional-extract-guards-v3` + `functional-conservation-obligation-evidence-v1`; five categorized checks, deterministic evidence anchors recomputed at check time, legacy keys mirrored); unified failure semantics `execution_status ∈ ok/partial/failed` shared by product/cache/manifest/readiness (failed runs are never cached; `functional_direct_basis` blocks non-ok); single-source config readers `config.get_env*` (defaults live ONLY in `ENV_REGISTRY` — default NOT flipped); functional-level expert review authority stays in `ai_review_states.jsonl` (`level=functional`, FRE-id primary keys via extended `source_ai_requirement_id`; new `POST /functional-review-actions` + GET projection; analysis/clarification/agent_state/annotation projections wired); B-track claim store abstraction (`resolve_b_track_target_store`, publish/fold/lineage store-aware, direct mode publishes shadow bound to `functional_requirements.json`, queue execution honestly refuses in direct mode); `tools/ab_runner.py` is the flip-gate A/B runner (only switch differs, stub rejected, failures never PASS, per-document verdicts). Pending before default flip: WS0 human truth set, document_ready flip semantics, functional claim queue execution, real-corpus A/B numbers. Append-only rewrite: attempt log/verifier WAL (~370x on 300 events, compaction env thresholds registered in `config.ENV_REGISTRY`), enrich cache JSONL (`analyze-enrich-cache-v3`), translation sidecar journal (main JSON format unchanged). Hot paths: `load_committed_effective_snapshot_cached` for `/table-reviews`+`/document/pdf`, memoized `/ai-extraction-status`, slice-before-enrich `/requirements`, single-hash stage fingerprints, marker-contract cache, background startup maintenance after readiness. `llm_client` disables a tool after 2 consecutive same-tool errors instead of aborting paid rounds. `review_state`/`desktop_tasks` os.replace unified to 8-attempt linear backoff; burst-coalesced `fold_effective_ledger`; PID-liveness stale-lock stealing; UI auto-POSTs `/claim-maintenance` on recovery-pending 503 then replays. merge-to-main requires the golden `out/` regeneration workflow (multiple behavior versions bumped).

Cross-page parameter table source alignment (2026-08-15): `TABLE_STRUCTURE_VERSION="table-structure-v10"` combines the two independent v9 behaviors: `(a)..(z)` composite headers and deterministic headerless continuation detection for consecutive sibling dotted clauses such as `6.12 → 6.13`. `CLAIM_ANNOTATION_VERSION="claim-annotation-v17"` publishes structured `table_context.fields`; the Vue source panel groups clause/specification/values and retains the flattened authoritative text in an audit disclosure. A current catalog paired with stale B-track extraction is exposed honestly as `catalog_only`, never with inherited stale coverage.

DOCX cell extraction + claim-authoritative table review (2026-08-04, design `C:/Users/YYHwudi/Downloads/2026-08-04-docx-table-cell-extraction-design.md`): `docx_table_parser.py` restores OOXML physical grids (`gridSpan`/`vMerge`/`gridBefore`/`gridAfter`), nested tables, paragraphs/lists/manual breaks, and honest parse-incomplete reasons (`DOCX_TABLE_PHYSICAL_VERSION="docx-table-physical-v1"`). `table_cell_dispositions.jsonl` (`table-cell-disposition/v2`, rules `table-disposition-rules-v2`) gives every canonical non-empty cell exactly one `target/context/composite/excluded/review` disposition. B-track prompts consume structured table leaves plus `term_defs`/`ref_texts`, not flattened table blobs (`ANALYZE_PROMPT_VERSION` unchanged; `AI_EXTRACT_PROMPT_VERSION="ai-extract-v24"`, `EXTRACT_GUARDS_VERSION="guards-v22"`). Scheme A uses `claim-catalog-v12`, candidate-decision writer v3 (frozen v1/v2 replay), and `table_claim_authority.py` (`table-claim-authority-v1`): claim structural decisions are the sole terminal authority; B-track dispositions/review state are deterministic projections. `target/composite` delegates to claim promotion, `context/excluded` to claim exclusion confirmation; partial batches report completed/remaining cell IDs and retry only the remainder. There is no production LLM table-structure classifier in this version; `llm_assisted` remains a reserved audit value. Table-scoped deterministic regeneration (`table-local-recompute-v1`) does not rerun the document.

Claim paid-work recovery (2026-08-01): queue and verifier accounting fan out through `.claim_budget_checkpoint.outbox.json`. The same cumulative budget transition is durably projected to `claim_reextract_attempts.jsonl` and the verifier WAL before the outbox is removed. GET paths fail closed and never recover it; queue execution, startup maintenance, or `POST /claim-maintenance` recover under write locks. `CLAIM_REEXTRACT_ATTEMPT_VERSION="claim-reextract-attempt-log-v3"`.

Result package layout (2026-08-02/03): new desktop runs write a `result-package.json` marker (`ratomizer-result-package/v1`, layout `result-layout-v1`) plus `.ratomizer/{pipeline,state,cache,logs,stages}`; the root holds only registry-published human deliverables. **Addressing rule: all state/cache/log/stage paths MUST go through `result_package.governed_artifact_path` (or `package_artifact_path` for registry artifacts) — never hardcode filename joins against the output root.** Under `package_v1` those files live in `.ratomizer/state|cache|logs|stages`, so bare `root / "review_states.jsonl"`-style joins silently mis-address (this caused the 2026-08-03 B1 startup-maintenance skip). `governed_artifact_path(..., for_write=False)` is the pure-resolution read form; only `for_write=True` may create directories. Marker loading validates against `schemas/result_package.schema.json` (top-level whitelist, `package_id`/`tool`/`warnings`); `load_result_package(root, verify=True)` recomputes deliverable/completion-evidence SHA and is wired to 「打开已有结果」(`/result-package?verify=1`, `result-package-status --verify`) — mismatch surfaces `result_package_modified` ("结果文件已被修改"). The frozen PySide6 `gui/` (`gui/requirements_model.py`) reads legacy flat paths only and is compatible with legacy outputs, not `package_v1`.

Facsimile geometry + table-row/cell zones (2026-08-01, same branch): `_resolve_pdf_geometry` backfills block geometry for page-less docx/xlsx blocks and carries row/canonical-cell geometry without inventing coordinates. Row zones remain mutually exclusive; merged cells render only at their canonical anchor. The app and standalone HTML expose row and cell context plus table-cell claim chips, including title/header cells. Current annotation contract is `claim-annotation-v16`, with producer stamp `doc_annotation_export/v16-cell-claim-projection`.

Quality-first unit routing M0-M4/M6/M7 core (2026-08-17, same branch, plan `docs/quality-first-unit-routing-complete-plan-2026-08-16.md`): `extraction_units.py` (`extraction-unit-planner-v2` since 2026-08-17e — COSEM table-level context sinks into row/cell `cosem_structured` roles; schema `extraction-unit/v1`) deterministically builds the A/B-shared content units from blocks/table_items/cell_items/dispositions plus term/ref context units; every non-empty canonical cell is traceable to exactly one unit (row units carry `covers_cell_ids`; conservation hard-validated). `unit_router.py` (`unit-router-v2` since 2026-08-17e — COSEM structural table units with obligation modals route `a_track` via `cosem_table_a_priority`; schema `unit-routing-decision/v1`) is the zero-LLM router (a_track/b_track/mixed/context/review): hard A = validated OBIS/whitelisted class_id/COSEM context (attribute/access words need a core signal or COSEM table context; named class words like "Data" only count under COSEM table context), hard B = obligation modals (functional_drilldown authority) or strong normative pattern; weak signals → review (materialized, never dropped); definition/reference/heading are always context even when quoting shall-text. CLI `route-units --out <dir>` writes governed `unit_routing_decisions.jsonl` (registered in `_ARTIFACTS`); no execution change. M3: `routing_gaps.py` (`routing-gap/v1`, stable hash gap_id, merge-dedupe), `quality_gates.py` (`quality-gates-v1` — read-only projection of EXISTING authorities: functional conservation/execution_status, disposition closure, routing review, package completion → pass/retry_local/needs_review/needs_work), `routed_execution.py` deterministic mixed-merge core (obligation_identity dedupe; whitelisted `implementation_constraints` via extract_parameters + CLASS_NAME_TO_ID + normalized OBIS; one authoritative requirement per obligation; narrative provenance never fakes LLM output). M4: `pipeline_contracts.py` + `pipeline_plan.py` (`ratomizer-pipeline-plan/v2` with sha256 fingerprint; policies quality_first|force_a|force_b|full_dual_audit|legacy_combined) — CLI `plan`; **default stays `legacy_combined`** until the router passes real-corpus gates (plan §31); envs `RATOMIZER_EXECUTION_POLICY`/`RATOMIZER_TRANSLATION_MODE`/`RATOMIZER_BUDGET_MODE`/`RATOMIZER_UNIT_ROUTER_RULES` registered. M6: `generate_annotation_translations(..., translation_mode=off|markers|full)` — off does zero-call cache maintenance only (proven via counting chat), full adopts accepted `document_translations.jsonl` entries (same `translation_key`, same guards, provenance `full_translation_sidecar`); `export_annotation_bundle(translation_mode=...)`; env default "full" maps to legacy marker behavior so the default surface is unchanged, explicit off/markers is enforced; `full_translation_enabled()` returns False under translation mode off/markers. M7 first batch consumers migrated: `spec_enrich.append_cache/read_cache` and `ai_extract.append_cache/read_cache` now write through `PaidCacheStore` (`record_many` single-lock rewrite + `from_file` for already-governed paths); read side accepts BOTH legacy top-level rows and paid-cache-store payload rows (legacy caches stay valid, hit semantics unchanged; same-fingerprint last-wins preserved). Phase2 item 2b (2026-08-17c): `ab_runner --warm-a-cache <dir>` copies a kept A_atoms `ai_extract_cache.jsonl` into the fresh A workdir before the chain — the full A chain still executes end-to-end (only extraction hits cache, zero paid), route/model mismatches naturally miss, source+sha256 recorded in `a_warm_cache`, missing file fails loudly; 2 new tests (60/60). Phase2 probes (2026-08-17c, ~3-5 RMB): clause_family strategy FIXES the flash duplicates root cause on a 56-section targeted subset (probe2, artifacts in out/probe3-items.json); `functional-extract-guards-v6` strips `[TBL-NNNNNN]` table-caption markers at the conservation check sites (quote matching + preservation baseline — LLM quotes carrying the marker caused fake quote_matches_no_block, and table-id digits caused fake blocking number losses; tests/test_functional_guards_v6.py). Residual failures concentrate in COSEM/event-catalog TABLE blocks which belong to A-track/context per the plan; section-level routing proved unviable (truncated section_path id collisions, e.g. 50+ sections share `2 20 Control of` — vacuous validation, code reverted); the correct granularity is block/unit routing via extraction_units (M4/M5 wiring). WS0 gate executed and FAILED (2026-08-17, user-authorized truth = Canna29 xlsx Chinese Analysis column, 190 rows, runbook example thresholds; `docs/ws0-gate-result-2026-08-17.md`): exit 2 → RATOMIZER_EXECUTION_POLICY default stays legacy_combined per §31. A-track ran the full cold chain (343 sections → 996 atoms → 1847 final rows) but the gate's XLSX reader missed the body column on template V2.3.12 sheets (alias gap, fail-closed FAIL); B-track functional-extract conservation failed (duplicates=6, the known flash doc-level failure); the key hit HTTP 402 near the end. ab_runner fixes from this run: PARSED_ARTIFACTS now includes table_cell_dispositions.jsonl, and CLI runs need PYTHONPATH=. Reader follow-up (2026-08-17b, phase2 handoff item 2, zero-paid): the gate's final-XLSX reader is now template-calibrated — `_load_template_extents` computes per-sheet last-nonempty-row bounds from `--template` (same authority as `template_writer._next_seq`), rows within bounds are template-owned content (annex list sheets 计量列表/费率列表/事件列表 etc. + requirement-sheet sample rows) and are separated into `template_rows_skipped`/`template_only_sheets` instead of counting as produced rows or triggering missing-body/empty-body FAILs; produced rows = appended rows only. 计量需求's split type columns (1P2W_SP/3P4W_DC/…) are read via `template_writer.WRITER_COLUMN_CONTRACT` (`REQUIREMENT_SHEET_SIGNATURE` = 序号+子模块; body=6/notes=7/section=9) when body aliases miss — the read position is exactly where the writer writes. Report schema `ab-runner-report/v3`. Budget mode (plan §14, commit item 9): `llm_budget.budget_mode()` resolves `RATOMIZER_BUDGET_MODE` (off|observe|enforce, default off). off = byte-identical legacy behavior (`RATOMIZER_LLM_BUDGET` alone governs, and enabling it keeps the enforcing semantics); observe/enforce force the ledger on — observe records every call, warns on over-limit (`[budget:observe] ... 放行不阻断`) and NEVER raises (exhaustion markers still recorded so the cost facts stay truthful), enforce keeps the existing pre-flight LLMBudgetExceeded interception. Tests: test_budget_mode.py. M8 first slice: `llm_client.call_context(stage=, processor=, unit_id=, parent_attempt_id=)` stamps every llm_trace row with a `context` key (contextvar; propagates through `submit_with_context` thread pools; absent context leaves trace shape unchanged) — provider attempts are now attributable to stage/processor/unit per §18. §20 frontend: App.vue settings gained a business "交付物" section with 翻译交付模式 select (`ratomizer.translationMode.v1`, default full=unchanged behavior; off/markers drop full-translation from the chain and pass `translationMode` through the Electron bridge → `desktop_tasks chain --translation-mode` → chain_task drops the stage with ledger accounting + export enforcement); A/B technical toggles (llmReview/aiExtract/assemble) moved into a collapsed 高级 details block (data-testids preserved); post-run the UI fetches `GET /unit-routing` (new api_server endpoint, shadow read-only) and appends the unit-routing counts line to the run summary; `ui/src/api-client.ts` gained `loadUnitRouting`/`UnitRoutingPayload`; verified with `npm test` (277 tests incl. 3 new in `DeliverySettings.spec.ts`) and `npm run build`. §22 result-package completion: `_completion_evidence` now evaluates `quality_gates.evaluate_document_gates(root, completion_scope_stages=requested_stages)` — overall needs_work refuses completion (ResultPackagePartialError, attempt stays running; conservation/execution gates only apply when functional-extract is in scope; the package-completion gate is skipped as self-referential; no-table docs pass closure as N/A); every completion records a `quality_gate_snapshot` evidence entry (new optional second entry in `schemas/result_package.schema.json` + validator; old single-entry markers remain valid); needs_review items are recorded, not blocking (their blocking semantics stay with their own authorities). M8 full: `llm_job_runner.py` (`LLMJobRunner.run/run_many`, `llm-job-runner-v1`) unifies single-shot/batch paid-call mechanics — route resolution via the existing `config_for_route` authority, fingerprint→PaidCacheStore (hit=zero provider calls, success-only writes), LLMRequestBudget passthrough, retry taxonomy (connection/remote), usage/provenance, and a governed attempt ledger `logs/llm_job_attempts.jsonl` (registered) whose rows carry stage/processor/unit_id + outcome (cache_hit/initial/json_repair/error) + tokens/duration/cache/model; `call_context` attribution flows into llm_trace in the same call; batch uses `submit_with_context` and reports ok/failed/cached/tokens with the §3.5 ok/partial/failed semantics; **LLMBudgetExceeded propagates** (budget exhaustion is control flow, callers keep their own degradation semantics). Consumers migrated so far: (1) `doc_map.run_doc_map` — the single LLM pass goes through the runner (stage=doc_map/processor=structure_hypothesis; injected-chat tests keep exact closure semantics via adapter; real route keeps the same chat_json path incl. max_truncation_escalations=1 and the document-budget hook; the budget-exhausted → honest-unavailable branch is preserved by the passthrough), and `doc_map_cache.jsonl` now reads/writes through PaidCacheStore (dual-format legacy rows stay valid). (2) `spec_enrich` — `enrich_one`/`_enrich_batch_unit` accept an optional runner (production entry `enrich_requirement_lists` constructs it from out_dir; test/direct calls without runner keep the legacy direct-chat path byte-identical); failed jobs RE-RAISE the original exception (`LLMJobResult.exception`, in-memory only) so LLMConnectionError-driven fast-fail/circuit-breaking semantics are preserved; successful attempts land in the ledger with stage=assemble/processor=spec_enrich and the job fingerprint reuses the existing cache `fingerprint()` identity. (3) `spot_extract._llm_spot_rows` — the chat closure is runner-backed so EVERY round of the critique_section self-check loop is its own job (stage=spot-extract/processor=critique_section, unit_id=spot:<block>[:R<row>]); failures re-raise the original exception preserving fail-loudly SpotExtractUnavailableError semantics; spot stays cache-less (user-targeted operation). All three migrations keep direct/test call paths byte-identical where no runner is passed. M9 convergence slice: `paid_cache_store.read_dual_format(path)` is now the SINGLE dual-format cache reader (legacy top-level rows + paid payload rows, torn-tail recovery, loud on mid-file corruption per io_utils discipline) — spec_enrich/ai_extract/doc_map no longer carry three hand-rolled copies. M9 cut 2 (2026-08-17d): `desktop_tasks.parse_args` (252 lines, zero module deps — AST-verified) + `build_requirement_library_task` (76 lines) moved verbatim to new `desktop_task_args.py` (registered in pyproject py-modules after test_packaging_smoke caught the omission) with full re-exports; desktop_tasks tests 113/113 + full suite 3953/4-pre-existing-golden. Full-target patch scan saved to out/m9-patch-targets.json (20/32/25/7 targets across api_server/desktop_tasks/ai_extract/claim_review_actions) — remaining large symbols all have intra-module deps, so further cuts need dependency-cluster moves (per blueprint, independent sessions). M9 first file split executed per `docs/m9-split-plan-2026-08-17.md`: the annotation-translation subsystem (47+ symbols: strategy versions, guards, journal/sidecar IO+locks, batch translation, orchestration incl. `generate_annotation_translations`) moved verbatim from `doc_annotation_export.py` (5473→4457 lines) into new `annotation_translations.py` (1134 lines) with full re-exports in dae; shared render state (`_active_translations`/`_collected_marker_texts` + their `global` rebind) stays in dae, generate's no-texts fallback lazily imports dae; `_fabricated_translation_tokens` is invoked THROUGH the dae namespace because tests patch it via `patch.object(dae, ...)` (alias-imported patch targets defeated the initial scan — the plan doc now records that hazard); verified by test_doc_annotation_export (158) + test_translation_mode (7) + dependents (256) + full suite 3929/4-pre-existing-golden. M9 first slice: `docs/adr/2026-08-17-quality-first-unit-routing.md` records the eight load-bearing decisions (unit single-source, zero-LLM routing, claim-queue reuse for escalation, successful-only paid cache, unified job runner, translation-as-delivery-option, gates-as-completion-evidence scoped per run, no default flip without WS0 truth). M5 local escalation: `routing_escalation.py` (`routing-escalation-v1`) wires actionable gaps (`targeted_secondary_route`/`targeted_reextract`) into the EXISTING claim queue — matches gap block-jurisdiction (gap `block_ids` or unit `source_block_ids`) against published pending proposals (`lifecycle=open`, `parent_block_id`) and executes via `execute_claim_queue_proposal` (shared CAS/WAL/budget/idempotency; stable key `gap-{gap_id}-{salt}`); no matching proposal → `no_matching_proposal` honestly (claim anchors are never fabricated); `expert_review`/`needs_work` gaps never auto-execute; `routing_gaps.gaps_from_functional_product` builds block-anchored gaps from failing conservation checks (evidence_mismatches/duplicates → targeted_reextract; anchor-less uncovered obligations/execution_status → needs_work); audit rows in governed `routing_escalations.jsonl`; E2E proves queue-driven clause-family reextract with unaffected-FRE byte stability. M0: `tools/m0_baseline.py` (DeepSeek `openai_compatible` route; per-stage trace+budget deltas; cold/warm; ABNT results in `docs/m0-baseline-abnt-summary-2026-08-17.md`: cold = 350 calls/1.42M tokens of which full-translation is 344 calls/1.18M (82.6%); functional extract is doc-level 2 calls/235,798 tokens and conservation FAILS deterministically on deepseek-v4-flash (duplicates=6 → execution_status=failed), and failed runs are never cached so warm re-pays extraction 235,798 tokens while translation sidecar fully reuses warm 1,226s→0.4s; llm_trace is the cost authority — the budget ledger rebuilds sheets across per-stage invocations and under-attributes translation/export). New tests: test_extraction_units / test_unit_router / test_routing_gates_merge / test_pipeline_plan / test_translation_mode / test_paid_cache_store.

§17 unit-routing execution wiring (2026-08-17e, same branch, commits 78d3d83..c59d902; full entry in CLAUDE.md): `functional_extract.apply_unit_routing` (`functional-unit-routing-v1`, clause_family strategy ONLY) routes pure-table clauses (all declared blocks are table blocks AND no b_track/mixed units on them) out of the B-track input AND conservation baseline; the skip list/counts/version identity land in the product's `unit_routing` block (never silent; `table_parse_inputs_missing` → honest unavailable + full input fallback). Fingerprint: `unit_routing_key` enters only the clause_family key space — legacy is byte-identical. `functional_reextract` applies the same deterministic filter for its clause pool/conservation baseline and refreshes the audit block. Zero-cost verification PASSED (probe3 131-item replay: all 50 failing-cluster clauses routed out; full corpus 358 → 186 extracted/172 routed out/3 non-COSEM tables kept). M9 cuts 3-5 (2026-08-17e): `api_server_support.py` (parse/auth + quote-matching + review-summary helpers, 19 symbols; BASELINE_BARE_JOINS migrated), `ai_extract_verify.py` (second-pass semantic verify cluster, 7 symbols incl. shared `_append_note`), `claim_events_journal.py` (event-log journal: `_scan_event_log_unlocked`/`ClaimReviewActionError`/`EventLogSnapshot`, 11 symbols) — all verbatim moves with original-name re-exports, all test patch targets stay in the origin modules, full suite 3972 OK per cut. Paid gate rerun pending on API key; gate command MUST set `RATOMIZER_CONTEXT_PACK_STRATEGY=clause_family` (runbook updated).

## Test & verify commands

Backend (run from repo root):

```powershell
python -m unittest discover -s tests   # NOT pytest — pytest is not installed; module-level def test_* is silently skipped. Tests MUST be unittest.TestCase.
```

Frontend (run from `ui/`):

```powershell
npm test         # vitest
npm run build    # vue-tsc --noEmit + vite build (typecheck lives here)
```

On this machine PowerShell blocks `npm.ps1` (execution policy) — use `cmd /c "npm test"` or fix the policy; this also affects `npm run desktop:pack`.

README's `python -m pytest -q` is stale; trust the unittest command above.

- In the main checkout (where `out/` exists): 0-skip run requires env `RATOMIZER_HISTORICAL_SAMPLE="C:/Users/YYHwudi/Desktop/Canna-29/eval_assets/test18_functional_synthesis_sample.json"` (machine-local path, else 1 honest skip). In isolated worktrees, `out/` is git-ignored so the 5 golden tests also skip — a green run there does NOT cover golden regression.
- Golden regression (`golden_sets/abnt_nbr_16968_v5/golden_summary.json`) is a frozen baseline — never change it without itemized justification. Baseline output in `out/abnt_nbr_16968_atomizer_v5/` was generated with **three seed `--kb` files + domain-pack**, NOT the single compiled KB; regenerating with the wrong KB causes fake drift.
- GUI tests skip if PySide6 is absent.

## Hard constraints (violations have caused real bugs)

- **GUI = `ui/` (Vue3+Electron). `gui/` (PySide6) is FROZEN** — kept runnable but never extended. `review_actions.py` lives at repo root, not under `gui/`.
- **Cache fingerprints must include deterministic post-processing versions.** Cached results store post-guard output, so if you change guard/verify behavior, bump `EXTRACT_GUARDS_VERSION` (and equivalents) — otherwise old caches silently bypass new behavior.
- **KB dual-track is deliberate:** runtime (CLI default + GUI preset) uses single `knowledge_bases/compiled_from_obsidian.json`; golden baseline uses the three seed KBs. Don't "unify" them.
- **Never commit:** Blue Book PDFs, company template xlsx, customer documents, eval assets with client wording, API keys. Keys are env-vars only; the mimo endpoint requires `x-api-key` header (`llm_client` sends both).
- Machine-local test assets (this machine only; parameterize if tests ever run elsewhere): `C:\Users\YYHwudi\Desktop\Canna-29\` (ABNT docx/pdf, Blue Book part 1/2 PDFs, company template).
- Anti-hallucination discipline: structured fields (OBIS, class_id, access) are deterministic-join only; LLM enrichment fills narrative fields only, fabricated codes/numbers are rejected. "Better to miss than to guess" (宁漏勿错).
- **Provenance is never falsified:** route degradation must record `route_requested` truthfully; stub output must never be labeled as LLM output; cached/merged results keep their true origin.
- **Phase 1 is zero-LLM:** the rule loop has `tokens_max=0`. Its resample action queues the current omission as `needs_extraction` and records `skipped`; only an explicit external `allow_llm=True` tool call may delegate to `targeted_reextract`. A queued action must never be reported as completed extraction.
- **Shared state files** (`review_states.jsonl`, `ai_review_states.jsonl`, `run_manifest.json`, append-only caches) require cross-process lock + atomic replace with `PermissionError` retry (Windows readers block `os.replace`). Copy the existing pattern in `review_state.py` / `ai_review_actions.py` / `desktop_tasks.py`; never bare-append without a lock.

## Environment gotchas

- **Node 24 breaks `extract-zip`** (electron install silently writes nothing; `npm install` won't fix). Fix: `Expand-Archive` the cached electron zip from `%LOCALAPPDATA%\electron\Cache\<hash>\` into `ui/node_modules/electron/dist`, then write `ui/node_modules/electron/path.txt` containing `electron.exe`. Packaging via electron-builder is unaffected. Root fix: use Node LTS 22.
- Dev machine runs Python 3.14, but `pyproject.toml` only requires >=3.11 — don't hard-require 3.14 features. `requirements.txt` is runtime deps only; desktop packaging additionally needs PyInstaller (`pip install -e .[package]`, used by `cd ui; npm run desktop:pack`, which builds the backend first).

## Workflow conventions

- Implementation happens on `codex/*` branches in isolated git worktrees; user decides merge to main. Never push without user approval. Never commit unless asked.
- Golden six-item tests run only in a checkout where the `out/` baseline exists (main; they compare against the pre-generated directory, they do not regenerate it). After bumping any behavior version (`EXTRACT_GUARDS_VERSION`, `*_PROMPT_VERSION`, `ENRICH_GUARDS_VERSION`, `LLM_REVIEW_CACHE_VERSION`): merge to main, regenerate `out/abnt_nbr_16968_atomizer_v5/` with the three seed KBs + domain-pack, then run the golden tests — the merge is not final until drift is zero or itemized in `CLAUDE.md`.
- B-track (`merged_spec`/`analyze`, AI extraction) is the primary deliverable for prose-style/tender docs (no COSEM tables; rule layer is blind to noun-phrase specs). A-track (`assemble`, atoms+llm_review) is primary for DLMS profile docs.
