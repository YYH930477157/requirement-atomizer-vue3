# 批处理优化实施与转正验收报告

- 日期：2026-08-10
- 分支：`codex/batch-processing-optimization`
- 基线：`origin/main` @ `29afb45`
输入方案：`C:\Users\YYHwudi\Downloads\批处理优化实施方案.md`

## 1. 验收结论

翻译批处理已按 SBD 45 条盲评结论转为默认开启，默认批量为 10；复核批处理仍默认关闭。护栏修复、提示词语言要求、缓存/阶段指纹和低危审查项均已完成，SBD 主结果已使用真实 DeepSeek 路由清缓存重翻，并完成 20 条新旧人工对比。自动化测试门禁通过；方案列出的 `run_smoke.py` 在仓库中不存在，且本机未找到盲评 45 条原始结论文件，因此这两项外部证据门禁仍明确未关闭。

## 2. 实现范围

### 2.1 翻译批处理

- `RATOMIZER_TRANSLATE_BATCH` 默认 10；显式设为 0 回退旧 batch=8 行为，正整数硬上限 10。
- `RATOMIZER_TRANSLATE_BATCH_MAX_CHARS` 默认 8000；按条数和字符数双上限顺序贪心装包，单条超限不截断、独立发送。
- 批量请求使用 `translation-prompt-v4` 数组契约，要求逐条独立翻译并使用规范中文技术标准语体。
- 整批结构非法时最多拆半两层，仍失败再进入原有逐条/句段降级链。
- 缺失、重复、越界 id 均逐条 fail-closed，不污染同批合法结果。
- 每条译文独立检查受保护编码、数字与单位的新增和缺失；漂移结果绝不落为成功译文。
- 缓存仍按内容逐条保存；批大小和字符上限进入策略及阶段指纹；并发失效使用译文 SHA CAS，避免覆盖另一进程写入的新成功译文。
- 单条/句段降级策略升为 `annotation-translation-v3-segment-fallback`；交互式 API prompt 升为 `translation-prompt-v3`。
- 翻译护栏升为 `annotation-translation-guards-v3`：千分位并组和枚举标号在原文/译文两侧对称归一化；括号数字只有位于行首或句界时才按枚举剥除，正文语义数字继续受缺失检查保护。

### 2.2 复核批处理

- `RATOMIZER_REVIEW_BATCH=0` 默认关闭；2..20 开启，推荐 15。
- 只批处理 legacy single-shot 路径；默认 `review_pipeline.yaml` 的 tool-loop 始终逐 requirement 执行。
- 批量请求使用 `m2-review-v4-batch`，按原样 `requirement_id` 回填。
- 每条结果仍经过 schema、确定性政策 floor、修订文本受保护 token 漂移、split/merge 批内引用校验。
- 单条非法返回 `needs_expert`/`rule_stub`；整批结构非法最多拆半两层，再回退原有逐条复核。
- 缓存指纹绑定精确批成员集合、模型、prompt、执行器、策略、证据和有效批配置；拆半结果绑定实际成功子批。
- 第一批同步执行，连接失败会在并行 fan-out 前快速中止。

## 3. 自动化验证

| 验证项 | 结果 |
|---|---:|
| 首轮翻译/配置/结果包聚焦测试 | 312 tests OK |
| v3 返修后相关测试 | 335 tests OK |
| 隔离 worktree 后端全量 | 3427 tests OK, skipped=7 |
| 合并后 main 后端全量 | 3427 tests OK, 0 skip |
| 合并后冻结 golden | 6/6 OK，零漂移 |
| 修改 Python 文件编译 | 通过 |
| `git diff --check` | 通过，仅 Windows LF/CRLF 提示 |
| 前端 | 未改动，无需新增前端验证 |

全量命令使用仓库约定的 `python -m unittest discover -s tests`，并配置本机历史样本路径；7 项 skip 为环境/可选依赖类跳过，不宣称零跳过。方案提到的 `run_smoke.py` 当前仓库不存在，因此未执行该不存在的脚本；本机也未找到 `盲评结论-SBD.md`，无法把指定的 #18/#19/#20/#29/#49 或 45 条逐条复验冒充为已完成。

## 4. 真实 GLM 小样

模型：`glm-5.2`。密钥仅从本机 Claude Code 配置读入当前子进程的 `RATOMIZER_LLM_API_KEY`，测试结束即移除；未写入本报告、源码或仓库文件。

### 4.1 翻译 A/B

使用 10 条不含客户内容的 DLMS/COSEM 合成技术句，覆盖 OBIS、RS-485、bit/s、V、s、百分比、接口类和结果码。

| 指标 | 逐条基线 | 10 条合批 | 变化 |
|---|---:|---:|---:|
| HTTP 调用 | 10 | 1 | -90.0% |
| 总 tokens | 9083 | 1814 | -80.0% |
| 耗时 | 91.41s | 17.23s | -81.2% |
| 有效回填 | 10/10 | 10/10 | 持平 |
| 严格护栏拒绝 | 0 | 0 | 持平 |

合批译文逐条保留了全部受保护编码、数字和单位。该小样没有主动诱发漂移；漂移、缺 id、重复 id、结构非法、拆半和逐条回退由自动化故障测试覆盖。

### 4.2 复核 15+1

使用 16 条合成原子需求，按 15+1 两批调用，并对返回结果逐条执行生产 schema 与政策处理。

| 指标 | 结果 |
|---|---:|
| HTTP 调用 | 2 |
| 总 tokens | 7883 |
| 耗时 | 59.93s |
| 完成逐条校验 | 16/16 |
| 缺失/重复 id | 0 |
| 拆半或逐条回退 | 0 |
| 非法条目 | 0 |
| 决策分布 | accept 9 / revise 6 / needs_expert 1 |

安全类样本的确定性 policy floor 仍把其中 1 条提升为 `needs_expert`，证明合批没有绕开逐条政策层。默认 tool-loop 未参与该试点，因为其工具证据和逐条 token 预算不可合并；相关禁批契约由自动化测试覆盖。

## 5. SBD 真实重翻与人工对比

- 已删除 SBD 主结果包中的旧 `annotation_translations.json`（621 条、旧策略 v2），并保留同时间点的结果副本缓存作为旧译文对照基线，不改写、不删除。
- 真实路由使用 `deepseek-v4-flash`；API key 仅注入单次命令环境，未写入源码、文档、缓存元数据或仓库文件。
- 首轮导出实际产生 539 个翻译标记：519 个通过 v2，20 个在批量、单条或句段回退后仍被拒绝；64 次批量调用、56 次回退调用，接口失败调用 6 次，总耗时约 17 分 40 秒。
- 复核发现 v2 会把正文语义括号数字误当枚举剥除，护栏随即升至 v3。真实缓存复验确认 2 条旧 accepted 译文存在该风险并转入重处理，随后对全部未通过项补跑。
- v3 补跑调用摘要：cached 517 / translated 7 / rejected 15，5 次批量调用、33 次回退调用，接口失败调用 2 次，总耗时约 11 分 32 秒。调用摘要是本轮路径计数，不等同于最终缓存净状态。
- 最终缓存共 539 条，策略为 `translation-prompt-v4-greedy-splithalf-b10-c8000`，全部护栏版本为 `annotation-translation-guards-v3`，最终状态为 accepted 526 / rejected 13 / unresolved 0。拒绝项使用原文回退，`unresolved=0` 不代表 13 条已成功翻译。接口汇总未提供 token 数，不补造成本数据。
- 从新旧缓存可稳定配对的已接受且译文变化条目中抽取 20 条人工检查：17 条明显改善、2 条好坏参半、1 条轻微中文冗余回归；该组生硬被动句命中从旧版 1 条降为新版 0 条。未把客户原文或逐条译文写入仓库。
- `document_annotation.html` 已于 2026-08-10 21:55:20 刷新，大小 1,743,735 字节。请求布局为 `pdf_original`，但本次导出未解析到 `source_pdf`，因此真实终态降级为 `optimized`，没有伪造 PDF 原版叠加成功。
- 发现护栏误拒、人工错误率或降级率上升时，可用 `RATOMIZER_TRANSLATE_BATCH=0` 单点回退，不得放宽护栏换吞吐。

## 6. 安全与仓库状态

- 未写入或提交 API key、客户文档、SBD 原文、Blue Book PDF、公司模板或本机评测资产。
- 未修改 `golden_sets/` 与冻结 `out/` 基线。
- 当前改动未提交、未推送、未合入 main，等待用户决定。
