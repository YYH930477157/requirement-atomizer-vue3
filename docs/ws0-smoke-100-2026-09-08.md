# WS0 门禁 100 节限量冒烟——FAIL（单条款 stub 回退），非 Go/No-Go 依据

**日期：** 2026-09-08（用户授权付费 + 成本裁定「只抽 100 节」）
**命令：** `PYTHONPATH=. RATOMIZER_LLM_API_KEY=<key> RATOMIZER_CONTEXT_PACK_STRATEGY=clause_family
python tools/ab_runner.py --parsed-dir out/abnt_nbr_16968 --template "…V2.3.12….xlsx"
--route openai_compatible --truth golden_sets/ws0_human_v1/truth.jsonl
--thresholds golden_sets/ws0_human_v1/thresholds.json --limit-sections 100 --keep-dirs
--out out/ab-gate-report-v8.json`
**退出语义：** FAIL（`--limit-sections` 部分语料冒烟——真值集覆盖全文档，本报告
**不得作为执行策略翻转依据**；`e4382bb` 的 help/docstring 已注明该口径）。

## 判定

- **整体 FAIL，唯一失败项：`B_direct: B 路执行不完整：execution_status=partial`**（mixed
  → fail-closed，设计语义）。threshold_violations=[]（B 先 FAIL，行级真值指标未进入判定）。
- **A_atoms 通过其门禁项**：100 节 → ai-extract（含 29 次 max_tokens 截断自动升级重试，
  全部成功）→ 935 FRE → 成文 xlsx 在场。A 的 conservation=False 为诊断位（A 轨判定不卡）。
- **B_direct**：100 条款经 unit-routing v8 出池后 47 条款进抽取；route=mixed——
  **唯一 stub 回退条款 = 「2 20 Control of」（BLK-000363）**，该条款包 LLM 调用失败退占位；
  其余 46 条款真 LLM 产物。

## 与 2026-08-17 门禁的对照（进步面）

| 失败面 | 08-17 | 本次（100 节冒烟） |
|---|---|---|
| B duplicates | **6（FAIL 主因）** | **0**（clause_family + routing v8 根治） |
| B binding/evidence | 当时的失败来源 | **全绿**（v8 逐字重复豁免本轮未被触发——该形态未出现） |
| A reader 别名 | FAIL（读取器缺口） | **通过**（08-17b 模板校准修复生效） |
| B 剩余失败 | — | **1 条款包 stub 回退 + 2 条否定词 preservation，全部集中在「2 20 Control of」表格病理条款族**（任务 D 领域） |

B 轨守恒唯一红类：preservation 2 条 blocking（negation token=cannot/not @
「2 20 Control of」表格碎片块 BLK-000365+…）——与任务 D 归因的 Security/Control of
列序打乱病理同族；`routing_gaps` 会将其按块锚分流为 targeted_reextract（e4382bb 前置
的 9affa30 接线）。

## 成本

DeepSeek `deepseek-v4-flash`；探针 85 token；A 100 节 + B 47 条款包 + 分析/成文。
工作目录未落 llm_trace（本跑无 trace 文件），精确账单不可得；按缓存产物量级估算
总花费在数万至数十万 token 区间（flash 单价下约人民币元级以内）。已停的全量跑
（12 节）花费极小且未复用（新工作目录）。

## 下一步（真实修复路径）

1. 「2 20 Control of」条款包 stub 回退：对该条款族做 targeted reextract（块锚
   BLK-000363 在场，M5 队列可执行；费用极小）→ 消掉 mixed → B 轨可重评。
2. preservation 否定词 2 条：任务 D 表格病理（列序打乱拼接）领域，按
   table-preservation-design §3 专家定点闭环处理。
3. 全量（343 节）正式门禁：以上收敛后进行，才是 Go/No-Go 依据。

**工作目录（--keep-dirs）：** `%TEMP%\ab-runner.8k0bn96r\{A_atoms,B_direct}`；
报告：`out/ab-gate-report-v8.json`。
